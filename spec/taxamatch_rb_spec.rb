require 'spec_helper'

describe TaxamatchRb do
  it 'has a version number' do
    expect(TaxamatchRb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
